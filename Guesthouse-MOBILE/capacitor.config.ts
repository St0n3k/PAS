import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.example.app',
    appName: 'guesthouse-spa',
    webDir: 'dist/guesthouse-spa',
    bundledWebRuntime: false
};

export default config;
